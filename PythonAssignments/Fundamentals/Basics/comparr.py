list_one = [1,2,5,6,2]
list_two = [1,2,4,6,2]

#if list_one == list_two:
#    print "The lists are the same"
#else:
#    print "The lists are not the same"

def comparr(list_one, list_two):
    if list_one == list_two:
        print "The lists are the same"
    else:
        print "The lists are not the same"

comparr(list_one, list_two)